rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Public read access to 3D model files
    match /models/{file} {
      allow read;
    }

    // Default: deny all other access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
