import 'package:flutter/material.dart';

class ConditionTool {
  final String id;
  final String name;
  final Color color;
  final double defaultSize;

  const ConditionTool({
    required this.id,
    required this.name,
    required this.color,
    required this.defaultSize,
  });
}